{% extends '::base.html.twig' %}

{% block javascripts %}
    {{ parent() }}

    <script>

    </script>
{% endblock %}

{% block body %}

    {% if page.displayTitle %}
    <h1 class="page-header">{{ page.title }}</h1>
    {% endif %}
    
    {{ page.content|raw }}

{% endblock %}