
{{ form_start(form, { attr: { action: path('fos_user_change_password'), class: 'fos_user_change_password form-horizontal' }}) }}

{{ macros.formFields(form, ['current_password']) }}

{{ macros.formFields(form.plainPassword, ['first', 'second']) }}

<div class="form-action">
    <button type="submit" class="btn btn-default btn-lg pull-right">
        {{ 'change_password.submit'|trans({}, 'FOSUserBundle') }}
    </button>
</div>

{{ form_end(form) }}
