{#

This file is part of the Sonata package.

(c) Thomas Rabaix <thomas.rabaix@sonata-project.org>

For the full copyright and license information, please view the LICENSE
file that was distributed with this source code.

#}

{% extends "SonataUserBundle:Profile:action.html.twig" %}

{% block sonata_profile_content %}

    {% include 'SonataUserBundle:Profile:tabs.html.twig' %}

    <div class="row">
        <div class="col-lg-7">
            
            {{ form_errors(form) }}
            
            <form action="{{ path('sonata_user_profile_edit') }}" method="POST" class="form-horizontal">
                {{ form_start(form, { attr: { class: 'form-horizontal' }}) }}

                {{ macros.formFields(form, [
                    'gender',
                    'firstname',
                    'lastname',
                    'nickname',
                    'dateOfBirth',
                    'website',
                    'biography',
                    'locale',
                    'timezone',
                    'phone',
                ]) }}


                <div class="form-actions">
                    <button type="submit" name="submit"  class="btn btn-default btn-lg pull-right">{{ 'common.save'|trans }}</button>
                </div>

                {{ form_end(form) }}

                <br/>
                <br/>
                <br/>
                <br/>
                <br/>
                <br/>
        </div>
    </div>

{% endblock %}